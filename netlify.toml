[build]
  command = "npm run build"
  publish = "dist"

# SPA роутинг — без этого /active, /history возвращают 404 при прямом заходе
[[redirects]]
  from   = "/*"
  to     = "/index.html"
  status = 200
